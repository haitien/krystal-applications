apiVersion: v1
kind: Service
metadata:
  name: {{ include "krystal-api-service.fullname" . }}
  labels:
    {{- include "krystal-api-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "krystal-api-service.selectorLabels" . | nindent 4 }}
