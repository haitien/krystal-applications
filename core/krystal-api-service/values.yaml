# Default values for krystal-api-service.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: krystal-api-copy
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "1.1.0"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

env:
  NETWORK:
    value: "mainnet,bsc"
  APP_REWARD_NETWORKS:
    value: "bsc,bsctestnet"
  DEFAULT_DB_HOST:
    value: "host.docker.internal"
  DEFAULT_DB_USER:
    value: "kuser"
  DEFAULT_DB_PASSWORD:
    value: "kpassword"
  MAINNET_DEFAULT_DB_DBNAME:
    value: "production_krystal"
  ROPSTEN_DEFAULT_DB_DBNAME:
    value: "ropsten_krystal"
  BSC_DEFAULT_DB_DBNAME:
    value: "bscmainnet_krystal"
  BSCTESTNET_DEFAULT_DB_DBNAME:
    value: "bsctestnet_krystal"
  SHARED_DB_DBNAME:
    value: "krystal_shared"
  OPERATOR_SECRET:
    value: "empty"
  JWT_SECRET:
    value: "empty"
  ROPSTEN_NODE_ENDPOINT:
    value: "wss://dev-ethereum-ws.knstats.com/v1/ropsten/node"
  BSCTESTNET_NODE_ENDPOINT:
    value: "https://data-seed-prebsc-1-s2.binance.org:8545"
  MAINNET_NODE_ENDPOINT:
    value: "https://ethereum.knstats.com/v1/mainnet/hpc?appId=local-jarvis"
  BSC_NODE_ENDPOINT:
    value: "https://bsc.knstats.com/v1/mainnet/geth?appId=local-jarvis"
  APP_SENTRY_ENABLED:
    value: "false"
  APP_SENTRY_DNS:
    value: "https://cdd217680dcf47e6bf29c54b50afe9c6@sentry-v2.knstats.com/31"
  VERIFIER_PRIVATE_KEY:
    value: "f147ee9cd304095dda55e5f0decc0f7c448068d7e9411af44855e2693e3c27aa"
  ETHERSCAN_API_KEY:
    value: "NPCPT9V8UP2T47X6RIDYT7ZBEHTRTM2P13"
