apiVersion: v1
kind: Service
metadata:
  name: {{ include "krystal-go-service.fullname" . }}
  labels:
    {{- include "krystal-go-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "krystal-go-service.selectorLabels" . | nindent 4 }}
